apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: foundry-idp
  labels:
    app: backend
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: backend
    component: api

